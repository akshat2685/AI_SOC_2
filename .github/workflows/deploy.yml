name: Production Deployment

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy Notification
        run: |
          echo "Triggering deployment for release tag ${{ github.ref_name }}"
